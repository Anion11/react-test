import { useNavigate } from 'react-router-dom'

export const useLogout = () => {
	const logout = () => {
		// removeFromLocalStorage('token')
		// removeFromLocalStorage('sso-site')
		// navigate(HOME)
	}

	return logout
}
